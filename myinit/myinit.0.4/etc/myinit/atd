#! /bin/sh
#
# The `atd' daemon.
#
DEPENDS='root'
PROG=/usr/sbin/atd
ARGS=""

case "$1" in

    start)
	test -x $PROG || exit 5

	mystart $DEPENDS || exit 1

	"$PROG" $ARGS

	exit 0
	;;

    stop)
	killall "$PROG"

	mystop $DEPENDS
	;;
esac
